{
  "id": "dataset.validate",
  "name": "Dataset Validator",
  "type": "tool",
  "description": "Validate BLUX-cA JSONL files for schema and audit-note compliance.",
  "entrypoint": "python tools/validate_jsonl.py",
  "inputs": [
    {
      "name": "paths",
      "type": "list[str]",
      "required": false,
      "description": "Optional files or directories to validate. Defaults to data/*.jsonl"
    }
  ],
  "outputs": [
    {
      "name": "status",
      "type": "string",
      "description": "Pass/fail summary for validation run."
    }
  ],
  "pillars": ["Responsibility", "Right Action", "Risk"],
  "owner": "blux-ca",
  "version": "0.1",
  "tags": ["dataset", "validation"]
}
